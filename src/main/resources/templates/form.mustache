{{> pageHeader}}
<div class="border">
    <h2>Mock ceilometer alarm</h2>

    <p>
        To register an alarm call via curl, simply do:
    </p>
    <code>
        curl -X POST http://localhost:8080/alarm/some_alarm
    </code>

    <p>
        Where <code>localhost</code> is the host running the application and <code>some_alarm</code> is the
        name of the alarm to be registered. </p>

    <p>Otherwise, simply fill in the name of the alarm below and submit the form.
    </p>

    <form action="alarm" method="post" autocomplete="on">
        <input type="text" name="alarm_id" value="manual" hidden />
        <input type="text" name="previous" value="unknown" hidden />
        <input type="text" name="current" value="alarm" hidden />
        <input type="text" name="reason" value="operator intervention" hidden />
        <input type="text" name="reason_data" value="just because" hidden />
        <label>
            Alarm name:
            <input type="text" name="alarm_name">
        </label> <input type="submit" value="Submit">
    </form>
</div>
<p>To see the totals: <a href="/totals">to date</a></p>
{{> pageFooter}}
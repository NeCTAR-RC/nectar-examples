{{> pageHeader}}
<div class="border">
    <h2>Recorded so far</h2>

    <div class="table">
        <table>
            <tr>
                <th> Alarm Name </th>
                <th> Times Called </th>
            </tr>
            {{#rows}}
                <tr class="{{cssClass}}">
                    <td><a href=/view/{{alert}}>{{alert}}</a></td>
                    <td>{{value}}</td>
                </tr>
            {{/rows}}
        </table>
    </div>
</div>
<p>to trigger an alarm: <a href="/alarm">manually</a></p>
{{> pageFooter}}